# THE ONE PIPELINE THIS REPOSITORY HAS, and it runs for a release request and nothing else.
#
# There is no push pipeline any more, and `ci-post-receive.yml` is what this file replaces.
# qits-projects keeps a backing branch `release/<id>` per open release request — an octopus merge of
# that request's sources, refolded whenever the set changes — and publishes `ReleaseRequestChanged`
# on every successful re-fold. The merge primitive that writes that branch fires no `post-receive`
# and so publishes no `SCMPublishCommit`: without this file the fold exists and NOTHING BUILDS IT.
# A fold the git host answered `unchanged`, and one that conflicted, publish nothing — so every
# arrival here is a new sha worth a build.
#
# DECIDED AT MAIN, BUILT AT THE FOLD. This file is discovered, parsed and selected at `main`'s head,
# always; only the run's branch and sha come from the payload, which is why `checkout:` names them
# as a pair — a sha with no branch whose history holds it is not something a step container can
# fetch. A release request therefore cannot alter the CI that gates it by folding in a commit that
# edits this file. Inside the step, $QITS_CI_BRANCH is `release/<id>` and $QITS_CI_SHA is the merged
# sha; the fold is already checked out, so nothing below fetches or switches refs.
#
# ONE STEP, AND IT IS THE GATING HALF. The verdict returns on the ordinary
# `BuildSuccessful`/`BuildFailed` with `commitSha` = the merged sha, which is what qits-projects'
# `BuildStatusListener` matches on together with `repoId`. There is no trailing `gating: false` half
# because this repository has nothing to publish from a fold — see below.
#
# NOTHING IS PUBLISHED FROM HERE, AND NOTHING EVER WAS. This bundle is not an npm package and not a
# tarball CI uploads: the consuming service carries this repository as a Quinoa submodule and builds
# the bundle into its own image. A release of this repository is a TAG, and what acts on that tag is
# the consuming service's `ci-event-upstream-frontend.yml`, which moves the gitlink and pushes a
# maintenance branch. So retiring the push pipeline drops no publication: there is none, and this
# repository needs no `ci-event-release.yml`.
#
# One ordinary sandboxed step, and no image: this repository does not ship a container. The app is
# packaged and served by qits-workspaces, which carries this repo as a git submodule at
# service/src/main/webui — Quinoa's ui-dir — and builds it into the service image, so this
# pipeline's job is to gate a release request's fold green (install, test, build), and the service's
# pipeline is where the bundle becomes deployable.
#
# The recipe below is qits-spa-home's, and that repo is where it is maintained; the two should keep
# matching. It matches in full again: this repository now carries an eslint.config.js and a `lint`
# script, so `npm run lint` is back in its slot between the install and the tests. It runs before
# them on purpose — a lint failure is cheaper to read than a test failure caused by the same typo.

event: ReleaseRequestChanged

when:
  # repoName, not repoId: the git host's storage coordinate is an opaque UUID since
  # the identity cutover, so a name match on repoId is not dependable. The event
  # echoes the public name; a rename updates this line in the same campaign.
  - repoName: { exact: qits-workspaces-frontend }

checkout:
  branch: backingBranch
  sha: mergedSha

steps:
  - image: qits/build-images/node-base:latest
    timeout-seconds: 1800
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this an early failure would be
      # swallowed by the next command's exit code. -u is load-bearing too: an unset registry var
      # must stop the step, not quietly resolve the @qits scope against npmjs.org, where these
      # packages do not exist and never will.
      set -eu

      # THE LOCKFILE is the one file this step edits, and npm leaves no alternative.
      #
      # npm pins a full resolved URL per package, and this lockfile is generated on the deployment
      # host, so every entry names a localhost port — an address that does not exist in this
      # container. `npm ci` fetches tarballs by that URL and never asks the configured registry for
      # them, so the environment below does not reach them: without this swap the install dies on
      # connection refused. The paths are identical on every address, so replacing the ORIGIN and
      # keeping the path is all it takes, and the integrity hashes are what make that safe — bytes
      # from the other address must still hash the same. The clone is this step's own; nothing is
      # edited that anyone will ever read again.
      #
      # THE BYTE PLANE IS TWO SERVICES SINCE THE SPLIT, WHICH IS WHY THE SWAP READS THE PATH.
      # qits-platform-mirror holds the npmjs pull-through cache; qits-artifacts hosts the @qits
      # scope. No single origin serves both, so the path is what tells them apart: an entry under
      # the hosted registry's own pathname — $QITS_NPM_REGISTRY_URL's, /artifacts/npm/npm/ — is a
      # @qits tarball and gets the hosted origin, and every other entry gets the proxy's.
      # Flattening all of them to one origin is what this step did until the split, and it sends
      # every @qits tarball to the mirror, which has never held one: 404, on every run.
      #
      # THE ORDER OF THE TWO EXPRESSIONS IS THE WHOLE TRICK. sed applies -e in sequence per line,
      # so the broad proxy swap runs FIRST and the narrow path-anchored one corrects the @qits
      # entries after it. The other order lets the broad expression undo the narrow one.
      #
      # npm's own knob for exactly this, `--replace-registry-host=always`, cannot do it against a
      # registry mounted under a path, and could not have told two registries apart either.
      # @npmcli/arborist glues rather than resolves (reify.js:
      # `registry.slice(0, -1) + resolvedURL.pathname`), so
      # http://qits-platform-mirror:8080/artifacts/npm/npmjs/ + /artifacts/npm/npmjs/zone.js/-/…tgz
      # comes out as …/artifacts/npm/npmjs/artifacts/npm/npmjs/zone.js/-/…tgz and every tarball
      # 404s. Measured on npm 10.9.4.
      #
      # Every address is derived from the injected env rather than spelled here: no repository
      # states a deployment's addresses, and the two this repo does state
      # (registry.dev.localhost:8080 for the @qits scope and mirror.dev.localhost:8080 for npmjs,
      # in .npmrc, for a developer on the host) are the ones being replaced.
      npm_hosted_origin=$(node -p 'new URL(process.env.QITS_NPM_REGISTRY_URL).origin')
      npm_proxy_origin=$(node -p 'new URL(process.env.QITS_NPM_PROXY_URL).origin')
      npm_hosted_path=$(node -p 'new URL(process.env.QITS_NPM_REGISTRY_URL).pathname')
      sed -i -E \
        -e "s#(\"resolved\": \")https?://[^/\"]+#\1$npm_proxy_origin#" \
        -e "s#(\"resolved\": \")https?://[^/\"]+($npm_hosted_path)#\1$npm_hosted_origin\2#" \
        package-lock.json

      # ENVIRONMENT, not a file. This repository COMMITS an .npmrc (registry.dev.localhost:8080 for
      # @qits and mirror.dev.localhost:8080 for npmjs, right for a developer on the host), and npm
      # ranks a project .npmrc ABOVE ~/.npmrc — so the `cat > ~/.npmrc` preamble the library repos
      # use would be written and silently ignored. `npm_config_*` environment outranks both files,
      # so it wins without touching the tree.
      env npm_config_registry="$QITS_NPM_PROXY_URL" \
          "npm_config_@qits:registry=$QITS_NPM_REGISTRY_URL" \
          npm ci --no-audit --no-fund

      npm run lint

      # The jsdom target. This image has no browser and CI is not the place to download one.
      npm run test

      npm run build
